% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engagement_plan.R
\name{validate_step_dependencies}
\alias{validate_step_dependencies}
\title{Validate inter-step file dependencies for a resolved step list (Wave 5
PHASE-05, S3).}
\usage{
validate_step_dependencies(
  steps,
  cfg,
  registry = step_registry(),
  strict = FALSE
)
}
\arguments{
\item{steps}{list -- resolve_step_list() output (possibly filtered).}

\item{cfg}{list -- the loaded engagement config.}

\item{registry}{list -- step_registry() output; overridable for tests.}

\item{strict}{logical(1) -- escalate previous-run reads to errors.}
}
\value{
character -- warning messages (empty when none); stops on a hard
ordering violation (and, when strict, on a previous-run read).
}
\description{
For each step's \code{requires_fn} paths: when some registry step produces the
path but appears in this run \emph{after} the requiring step, that is a hard
ordering violation and this stops. When no step in this run produces the
path, the step reads a previous run's artifact -- a warning naming the
file, escalated to an error under \code{strict = TRUE} (--strict-deps).
}
